# SimNIBS TI Violante 2023 reproduction report

This is the GitHub Pages publishing bundle for the static report. Open `index.html` locally or publish this directory to a GitHub repository.

The bundle contains the report and figure assets required for browser viewing. Large raw deliverables (HDF5, NIfTI archive, and related package files) are intentionally excluded from the public site.
